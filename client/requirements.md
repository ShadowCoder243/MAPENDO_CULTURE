## Packages
framer-motion | Complex animations and page transitions
date-fns | Date formatting for events and blog posts
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
- Images are located in `client/public/images/mr culturel/`. Filenames contain spaces and special characters.
- Admin dashboard requires authentication via Replit Auth (`/api/login`).
- The design uses a sophisticated "African Modern" aesthetic with Serif headings and earthy/gold tones.
